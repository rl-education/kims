# Task config file
task_config_file: "cartpole.yaml"

# Policy config file
policy_config_file: "ppo.yaml"

## Device config
device: 'cuda'

# test - if set, run policy in inference mode (requires setting checkpoint to load)
test: False
# used to set checkpoint path
checkpoint: ''

# disables rendering
headless: False
